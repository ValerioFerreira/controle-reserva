/**
 * app-params.js — arquivo legado do Base44 SDK.
 * Mantido vazio para não quebrar imports existentes.
 * O controle de sessão agora é feito via AuthContext.jsx + localStorage.
 */
export const appParams = {};
